import 'package:flutter/material.dart';

class MatrixView extends StatelessWidget {
  const MatrixView({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('Matriz EisenHower');
  }
}